{
  "name": "pinch-boids",
  "version": "1.0.0",
  "description": "Webcam pinch-controlled boids flock. Browser demo, architected for an embedded port.",
  "type": "module",
  "scripts": {
    "build:wasm": "wasm-pack build core --target web --out-dir ../src/core-wasm",
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "onnxruntime-web": "^1.20.1"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vite-plugin-static-copy": "^2.1.0"
  }
}
